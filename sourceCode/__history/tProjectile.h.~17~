//---------------------------------------------------------------------------

#ifndef tProjectileH
#define tProjectileH

#include "tPlants.h"
#include "tError.h"
#include <vector>
//---------------------------------------------------------------------------

using namespace std;

class TForm1;

class tProjectile : public TImage {
	private:
		int velocity;
		int damage;
		TTimer* projectileTimer;
		tPlants* originPlant;
	public:
		__fastcall tProjectile(TComponent* Owner, tPlants* shooter);
		~tProjectile();
		void moveProjectile(vector<tProjectile*> &projectile, int i);
		void __fastcall OnProjectileClick(TObject *Sender);
};
#endif

