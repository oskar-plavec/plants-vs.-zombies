//---------------------------------------------------------------------------

#ifndef tZombieH
#define tZombieH

#include "tProjectile.h"
#include "Unit1.h"
#include <vector>
//---------------------------------------------------------------------------
using namespace std;
class tZombie : public TImage{
	private:
		String zombieType;
		int zombieHp = 1;
		int attackPower = 1;
		int attackInterval = 1;
		float speed;

	public:
		__fastcall tZombie(TComponent* Owner, String type, int top);
		int getZombieHp();
		int getAttackPower();
		int getAttackInterval();
		float getSpeed();
		String getZombieType();
		void moveZombie(vector<tZombie*> &zombies, int i);
		void dealDamageToZombie(vector<nZombie> &zombies, vector<tProjectile*> &projectile, tProjectile* project, int z, int p, int damage);


};
#endif
